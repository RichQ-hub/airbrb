import styled from 'styled-components';
import { FlexAlignCentre } from '../../styles/common';

export const StyledPropertyTag = styled.div`
  ${FlexAlignCentre}
  color: #fff;
  font-weight: 300;

  svg {
    fill: #fff;
    margin: 0 8px;
  }
`

export const BulletPoint = styled.span`
  font-size: 0.4rem;
`
